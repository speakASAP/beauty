# Changelog

## 1.0.0 (2024-01-01)

### Added
- Initial release of logger package
- Integration with centralized logging microservice
- Automatic tenant context injection via middleware
- Child logger support for request-scoped logging
- Fallback to console logging if service unavailable
- Support for all log levels (error, warn, info, debug)
- Non-blocking log sending with timeout protection

