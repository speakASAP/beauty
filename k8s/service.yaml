apiVersion: v1
kind: Service
metadata:
  name: beauty
  namespace: statex-apps
  labels:
    app: beauty
    managed-by: k8s-migration
spec:
  type: ClusterIP
  selector:
    app: beauty
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      name: http
